{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd deploy-bundle && rm -rf node_modules index.js && cd .. && npm install && npx tsc index.ts && mv index.js deploy-bundle && cp -R node_modules deploy-bundle"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1573.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.2"
  }
}
